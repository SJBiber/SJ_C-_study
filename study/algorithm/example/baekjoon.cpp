#include <iostream>
#include <vector>


using namespace std;

int main()
{
    int N;
    int K;

    cin >> N >> K;

    vector<int>W(N,0);
    vector<int>V(N,0);
    vector<int>table(N*N,0);

    for(int i = 0 ; i < N ; i++)
    {
        cin >> W[i] >> V[i];
    }
    
    for(int i = 1; i < N ; i++)
    {
        for(int j = 0; j < N ; j ++)
        {
               
        }
    }



}